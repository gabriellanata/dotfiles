# pr-quick

Manual trigger: use the **git-operations** skill (assume it is already available). Run **only** `## Workflow: pr-quick`—every step through completion. Do not run the other workflows in that skill.
